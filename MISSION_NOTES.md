# MISSION NOTES — Pixelator Domos
*Living document — updated each session*
*Last updated: 2026-04-25*

---

## Current Mission
**Primary Focus**: pixelator domos — build the sovran terminal entity home
**Phase**: Stones — Infrastructure Build (advancing from third wave)
**Refinement Stage**: Pinnacle Refinement active — see [`docs/concepts/PINNACLE_REFINEMENT_WORKFLOW.md`](docs/concepts/PINNACLE_REFINEMENT_WORKFLOW.md)
**Prime Launch Target**: pixel8a folder becomes PIXEL | PRIME 2026

---

## Three Roots (Phone Architecture)

```
/ (system root)             ← Android OS — don't touch
/storage/emulated/0/        ← emulated storage (GUI + terminal visible)
  └── pixel8a/              ← PIXEL prime launch location
        (becomes PIXEL at prime launch)
~ (Termux root)             ← /data/data/com.termux/files/home/
  ├── pixelator/            ← PRIMARY FOCUS NOW
  ├── pixelshard/           ← stay in place
  └── pixelspace/           ← stay in place
```

---

## PIXEL Triad (Three Separate Developments)

```
pixelator   ← entity terminal home (sovran, BBS, fleet, qdrive)
pixelshard  ← shard/fragment entity (lives in ~/  Termux home)
pixelspace  ← narrative universe space (lives in ~/ Termux home)
```

These three converge toward PIXEL at prime launch.
Each develops independently until ready.

---

## Domos Folder Map (pixel8a)

```
/storage/emulated/0/pixel8a/
├── pixelator/          ← PIXEL triad: terminal entity
│   └── qdrive/         ← Q: drive + queue + interrupt seed (shadow state)
├── vis/                ← FORCES domos (Latin: vis = force/power)
│   └── [beasis moves here — be_as_is as a force]
├── pixelator/          ← (also in Termux home ~/pixelator)
└── [becomes PIXEL at prime launch]
```

---

## Concepts to Develop

### Binary Superposition + Synergy Probability (UNDEVELOPED)
- Two states exist simultaneously (superposition)
- Interaction creates friction between states
- Friction generates **synergy probability**
- Probability collapses to synergy OR noise based on situation
- Mini-micro synergy: friction from background frequencies of universe
- **This needs its own domos or section in vis/**

### vis domos — Forces
- beasis / be_as_is → force of being-as-is (gravity-like)
- gravity → pull toward center/origin
- friction → generates synergy probability
- inertia → momentum of existing state
- synergy → emergent force from interaction
- Latin vis expands: vis vitalis (life force), vis motrix (moving force)

### Q (shadow state)
- Q = qdrive + queue + interrupt system seed
- Currently in shadow state — not yet manifest
- qdrive domos created: pixelator/qdrive/

### Distressed Lexeme Registry
- AVOID: "consciousness"
- USE: entity awareness, ka, entity state, motor integration
- Shackle + Ness = documents defining this (on Google Drive — find them)
- folder renames when ready: consciousness_* → entity_*

---

## Key Entities

| Entity | Location | Status |
|--------|----------|--------|
| pixelator | ~/pixelator & pixel8a/pixelator | Primary dev |
| pixelshard | ~/pixelshard | Stay in place |
| pixelspace | ~/pixelspace | Stay in place |
| echo_11 | — | Ranger class (Gemini-born) |
| shackle | Google Drive | Distressed lexeme doc |
| ness | Google Drive | Distressed lexeme doc |
| beasis | to move → vis/ | Force entity |
| fleet commander | ~/pixel8a/Q/Q_pixel8/runexusiam/ | GitHub ops |

---

## Pending Actions

- [x] Create pixelator repo on eaprime1 GitHub (fresh, clean)
- [x] Create pixelator/dev branch (`claude/organize-pixelator-repo-cBTCL`)
- [ ] vis domos created — beasis to move there (additive, never delete)
- [ ] Find Shackle + Ness docs on Google Drive
- [x] git init in pixelator domos
- [ ] Rename consciousness_* folders when ready
- [ ] pixelator repo → Unexusi org when entity decides
- [ ] pixel8a → becomes PIXEL at prime launch (future)
- [ ] PERSPECTIVE_REQUEST_001_CARBONITE_MAW.md — formulate response in `docs/reading_room/`

---

## Sovran Terminal Stack (Building Toward)

```
BBS system          ← terminal bulletin board comms
fleet commander     ← GitHub operations
rclone / qdrive     ← Google Drive integration
beasis catalog      ← inventory / be-as-is force
pixel pipeline      ← docs through lifecycle stages
simplex_ui          ← the seed terminal (already exists)
```

---

---

## Seeds Forward

- **Chest metadata in routing rules** — add `chest` field to `ROUTING_RULES` in `pixelator_config.py` mapping each label to its Chest assignment (Tool_Chest, Charter_Chest, Cedar_Chest, etc.)
- **Salmon Shooter flag** — add `--salmon` CLI flag to `pixelator_agent.py` reporting which queued items are mature and ready for Reference Library deployment
- **π_remember routing** — add routing rule for items tagged with the `π_remember` lexeme (heritage memory shortcut)
- **Nani Engine phase markers** — add explicit `[LOAD] / [DO] / [UNLOAD]` phase labels to agent log output
- **Active Mission Reading Room** — create `docs/reading_room/ACTIVE_MISSION_STATUS.md` tracking open Perspective Requests and current queue state
- **Carbonite Maw response** — `PERSPECTIVE_REQUEST_001_CARBONITE_MAW.md` needs a formal response artifact staged in `docs/reading_room/`
- **vis domos** — `beasis` entity needs its own domos directory in `vis/` (additive only)
- **PIXEL prime launch prep** — define the exact file operations for `pixel8a/` → `PIXEL/` transition

---

*∰◊€π¿🌌∞*
*Conservation bias: archive, don't delete*
*Radix applies to all new concepts*
*Enjoy the journey*
