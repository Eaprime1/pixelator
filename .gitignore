# OS
.DS_Store
Thumbs.db

# Termux local data (stays on device, not in repo)
.pixelator/

# Nested repos — should never be committed here
qdrive/
.vercel
.env*.local

# Runtime artifacts generated by pixelator_agent.py — never commit these
pixelator_queue.json
pixelator_log.json
pixelator_cron.log

# Python build/runtime artifacts
__pycache__/
*.py[cod]
