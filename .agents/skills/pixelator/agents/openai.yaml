interface:
  display_name: "Pixelator"
  short_description: "Repo-specific patterns and workflows for pixelator"
  default_prompt: "Use the pixelator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true