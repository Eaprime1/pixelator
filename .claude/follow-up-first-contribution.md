# Follow-up needed: the first contribution

PR #133 (kabbersokhi-boop) is the first contribution from a genuinely new
contributor to actually land content, not just an automated fix. It's
reviewed and approved but not merged. Two things still need doing once the
Shepherd decides to merge it:

1. Merge #133.
2. Open the follow-up PR that PR #133's own body names: add a `partner_state:`
   YAML block (`charge_received`, `friction_named`, `next_stance`) to
   `turns/AAR.md`. This is the concrete "next stage" move for this PR —
   don't let the proposal stop at approved-but-unmerged.

Also pending: Rachaelisa claimed bounty #130 first but never opened a PR;
her claim is now stale since #133 covers the issue. She needs a reply, not
silence — see `.custos/README.md` for the full contributor picture.
